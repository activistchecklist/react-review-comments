---
'@activistchecklist/react-review-comments': patch
---

Refine docked sidebar styling so the dock container owns the visual chrome, uses an inset inner shadow, and adds left padding for better shadow breathing room.
